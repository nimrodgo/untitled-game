Godot 4.6.3 Web export template (single-threaded, release), bundled so the
mobile playtest build works without downloading export templates.
If you upgrade Godot, replace this file with the matching version from
Editor > Manage Export Templates (or delete it and clear the preset's custom template).
